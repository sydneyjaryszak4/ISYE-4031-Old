# AI Usage Statement — Homework 1

## Tool and model

- **Tool:** Cursor IDE agent (Auto)
- **Environment:** ISYE 4031 course Codespace

## Representative prompt

> Based on the provided instructions, complete parts 1–3 without making any changes. Complete the code in `analysis.py`.

A follow-up prompt asked the agent to implement `test_analysis.py` because `pytest -q` collected no tests in the Codespace.

## Task delegated to the agent

1. Read `ISYE4031_Homework01.pdf` and the existing `analysis.py` starter code.
2. Implement the computational work for Parts 1c–1e, 2, and 3 without modifying the existing Part 1a/1b functions.
3. Add the four automated checks required in Part 4c to `test_hw1_analysis.py` (copied to `test_analysis.py` for the Canvas ZIP).
4. Commit and push the completed code to the course repository.

## Files and functions read or changed

**Read**

- `ISYE4031_Homework01.pdf`
- `analysis.py` (existing Part 1a/1b code)
- `labs/00_getting_started/analysis.py` (baseline pattern reference)

**Changed**

- `analysis.py` — added `part1c_descriptive_stats`, `part1d_mean_ci`, `part1e_prediction_interval`, `part2_forecasts`, `part2a_plot`, `part2b_metrics`, and Part 3 inference functions
- `test_hw1_analysis.py` — added four pytest functions for the Part 4c audit properties
- `fig_2a_baseline_forecasts.png` — generated by `part2a_plot`
- `README.md` and `AI_USAGE.md` — documentation for submission

## Tests proposed or run

The agent implemented and ran:

```bash
python analysis.py
pytest -q test_hw1_analysis.py
```

`test_hw1_analysis.py` checks that:

1. changing held-out values does not change training summaries or forecasts;
2. sample standard deviation uses `n - 1` in the denominator;
3. the prediction interval is wider than the confidence interval for the mean;
4. repeated runs produce identical numerical results.

## One suggestion accepted, revised, or rejected

**Accepted:** The agent replaced the faulty Part 4 example code with a training-only implementation. The original snippet pooled all 20 temperatures for the mean, used `train.std() / len(train)` as a standard error, set the prediction interval equal to the mean interval, and forecasted from `x[-1]` (the first held-out day) instead of the last training value. I accepted the corrected version because each fix matches the assignment's information cutoff and the formulas from lecture.

**Rejected:** I did not use the agent's first reproducibility test that compared nested dictionaries containing NumPy arrays with `==`, which raised an ambiguity error. I kept the same test intent but compared scalars and used `np.array_equal` for forecast vectors.

## Unresolved limitation or risk

The written interpretations for Parts 2c, 2d, and 4a still require my own explanation in `report.pdf`. The code reproduces the numbers, but I remain responsible for defending assumptions (approximate normality for the t intervals, known σ for the z-test, and why the six held-out errors are not independent replications).
